const express = require("express");
const message = express.Router();
const {
    getMessage,
    SendMessage
} = require("../controller/messagecontroller");

message.get("/message/:email", getMessage);
message.post("/message/:email", SendMessage);

module.exports = message;
