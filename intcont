const getReceivedInterests = (req, res) => {
    const { email } = req.params;
    if (!email) {
        return res.status(400).json({ error: "Email is required" });
    }

    const sql = `
        SELECT
            i.ID,
            i.RequesterEmail,
            i.ProfileID,
            i.Intrest,
            i.CreatedAt,
            p.*
        FROM interested i
        INNER JOIN profile p
            ON p.Email = i.RequesterEmail
        INNER JOIN profile cp
            ON cp.RegisterID = i.ProfileID
        WHERE i.Intrest = 'Intrested'
          AND cp.Email = ?
        ORDER BY i.CreatedAt DESC
    `;

    db.query(sql, [email], (err, results) => {
        if (err) {
            console.error("Error fetching received interests:", err);
            return res.status(500).json({
                error: "Database error",
                details: err.message
            });
        }
        res.status(200).json(results);
    });
};

module.exports = {
    postIntrest,
    getInterested,
    getReceivedInterests, // add this
    AllInterested,
    updateInterest,
    getYesInterested,
    getNoInterested
};
